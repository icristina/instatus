@inherits Instatus.Integration.Mvc.ExtendedViewPage

@using Instatus.Core
@using Instatus.Integration.Mvc

@using (Html.BeginForm())
{
    @Html.DropDownList(WellKnown.RouteValue.Locale, new SelectList(Hosting.SupportedCultures, "Name", "DisplayName", SessionData.Locale))
    @Html.Submit("Change")
}