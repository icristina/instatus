@inherits Instatus.Integration.Mvc.ExtendedViewPage

@using Instatus.Core
@using Instatus.Integration.Mvc

@using (Html.BeginForm())
{
    @Html.DropDownList(WellKnown.RouteValue.Locale, new SelectList(Hosting.SupportedCultures, "Name", "DisplayName", Preferences.Locale))
    @Html.Submit("Change")
}