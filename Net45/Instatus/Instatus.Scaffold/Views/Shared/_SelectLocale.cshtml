@inherits Instatus.Integration.Mvc.ExtendedViewPage

@using Instatus.Integration.Mvc

@using (Html.BeginForm())
{
    @Html.DropDownList("locale", new SelectList(Hosting.SupportedCultures, "Name", "DisplayName", SessionData.Locale))
    @Html.Submit("Change")
}