@model WebSection

@{
    var webLink = Model.Links.First();   
}

<figure>
    <img src="@WebPath.Absolute(webLink.Uri)" />
    <figcaption>
        @Html.Tag("b", webLink.Title)
        @webLink.Description
    </figcaption>
</figure>