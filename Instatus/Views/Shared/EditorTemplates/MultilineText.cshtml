@model object

@{
    var attributes = Html.ValidationAttributes()
                        .AddNonEmptyValue("class", "input-xlarge");    
}

@Html.TextArea("", string.Format(ViewData.ModelMetadata.EditFormatString ?? "{0}", Model), attributes)